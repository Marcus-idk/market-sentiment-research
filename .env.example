# Market Sentiment Analyzer Environment Variables
# Copy this file to `.env` and fill in your actual API keys.

# Generator-friendly format:
# - Put the human description in comment lines immediately above each KEY=value line
# - Avoid inline comments after KEY=value

# --- LLM providers ---

# Required: OpenAI API key (for LLM analysis).
OPENAI_API_KEY=your-openai-api-key-here

# Required: Google Gemini API key (for LLM analysis).
GEMINI_API_KEY=your-gemini-api-key-here

# --- Data providers ---

# Required: Finnhub API key (market data fetching).
FINNHUB_API_KEY=your-finnhub-api-key-here

# Required: Polygon.io API key (news fetching).
POLYGON_API_KEY=your-polygon-api-key-here

# Required: Reddit API credentials (PRAW OAuth).
REDDIT_CLIENT_ID=your-reddit-client-id
REDDIT_CLIENT_SECRET=your-reddit-client-secret
REDDIT_USER_AGENT=your-app-name/1.0 by your-username

# --- Poller ---

# Required: Stock symbols to track (comma-separated list).
# Example: SYMBOLS=AAPL,MSFT,TSLA,GOOGL
SYMBOLS=AAPL,MSFT

# Required: Polling interval in seconds.
# Examples: 60=1 minute, 300=5 minutes, 600=10 minutes
POLL_INTERVAL=300

# Optional: Database path. Default: data/database/market_sentiment_analyzer.db
DATABASE_PATH=data/database/market_sentiment_analyzer.db

# Optional: Streamlit UI port. Default: 8501
# Used only when running `run_poller.py` with `-v`.
STREAMLIT_PORT=8501

# --- Logging ---

# Optional: Logging level name. Default: INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Log file path. Default: unset (console only).
# If set, logs also write to this file.
LOG_FILE=logs/poller.log

# Optional: Logging format string. Default: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
